import React, { Component } from 'react';

class Heap extends Component {
  render() {
    return (
      <div className="Heap">
        
        {this.props.heap}
    
      </div>
    );
  }
}

export default Heap;
